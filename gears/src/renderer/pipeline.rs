/* use vulkano::pipeline::{GraphicsPipeline, shader::GraphicsEntryPoint};

pub struct Pipeline {}

impl Pipeline {
    pub fn graphics(modules: &[GraphicsEntryPoint]) -> GraphicsPipeline {
        let

        GraphicsPipeline::start()
        .vertex_shader(shader, specialization_constants)
        Self {}
    }
} */
